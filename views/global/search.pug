extends ../layouts/main.pug
include ../mixins/poemBlock

block content
    .search__header
        h3 Searching by #{searchingBy}
    .search__poems
        each poem in poems
            +poemBlock({
                id: poem.id,
                title: poem.title,
                content: poem.content
            })